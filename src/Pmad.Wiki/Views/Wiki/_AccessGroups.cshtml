@model List<WikiGroupViewModel>

@if (Model.Count == 0)
{
    <span class="text-muted">@Localizer["All users"]</span>
}
else
{
    foreach (var g in Model)
    {
        <span class="badge rounded-pill text-bg-@g.ClassSuffix me-1" title="@g.Tooltip">@g.ActualLabel</span>
    }
}